<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key> 
    <string>3s-arm</string>
    <!-- The netplay launcher, not the game. Finder runs only what this key
         names, and a friend who has to host, join or rebind a key has no
         other way in; the launcher's PLAY button spawns 3S-ARM, which sits
         beside it in Contents/MacOS, for everyone else. Byte-identical to
         the launcher target's OUTPUT_NAME: a merely case-insensitive match
         boots fine on APFS and then fails `codesign --verify`. -->
    <key>CFBundleExecutable</key>
    <string>3S-ARM-Launcher</string>
    <!-- Installed from cmake/3s-arm.icns by the APPLE install() rule in
         CMakeLists.txt. Both halves are required and neither is optional:
         Finder shows the generic app icon if the key is missing OR if the
         file it names is absent from Contents/Resources. This pair was added
         by hand to the 2026-09-01 package and therefore did not survive into
         the next build -- keep it in the tree, not in a shell history. -->
    <key>CFBundleIconFile</key>
    <string>3s-arm.icns</string>
    <key>CFBundleIdentifier</key>
    <string>com.crowded-street.3s-arm</string>
    <key>CFBundleVersion</key> 
    <string>1.0</string>
    <key>CFBundlePackageType</key> 
    <string>APPL</string>
    <key>CFBundleSignature</key> 
    <string>????</string>
    <key>LSMinimumSystemVersion</key> 
    <string>11.0</string>
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.games</string>
</dict>
</plist>
